services:
  - type: web
    name: odds-shopper
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: ODDS_API_KEY
        sync: false        # you paste this in the Render dashboard — never committed to git
